version: '3.1'
services:
  positive-webserver:
    image: nginx:alpine
    container_name: positive-nginx
    working_dir: /app
    depends_on:
      - positive-php-fpm83
    volumes:
      - .:/app
      - ./.docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "${NGINX_PORT}:80"

  positive-php-fpm83:
    container_name: positive-php-fpm83
    build: .docker/php-fpm83
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - NGINX_PORT=${NGINX_PORT}

  positive-db:
    container_name: positive-db
    image: mysql:8.4
    volumes:
      - ${DATABASES_PATH}:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: 111
    ports:
      - "${MYSQL_PORT}:3306"